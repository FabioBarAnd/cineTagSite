import styles from './Rodape.module.css';

function Rodape() {
    return(
        <footer className={styles.footer}>
            Desenvolvido por Fabio com ajuda de Alura.
        </footer>
    )
}

export default Rodape;